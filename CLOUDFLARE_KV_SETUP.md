# 🔧 Настройка Cloudflare KV для синхронизации

Пошаговая инструкция по настройке Cloudflare Workers + KV для синхронизации туду-листа между устройствами.

## Шаг 1: Создание KV Namespace

### Вариант A: Через Cloudflare Dashboard (проще)

1. Откройте: https://dash.cloudflare.com/
2. Выберите ваш аккаунт
3. В левом меню: **Workers & Pages** → **KV**
4. Нажмите **"Create a namespace"**
5. Имя: `TODOS_KV` (или любое другое)
6. Нажмите **"Add"**
7. **Скопируйте Namespace ID** (понадобится для `wrangler.toml`)

### Вариант B: Через Wrangler CLI

```bash
# Установите Wrangler (если еще не установлен)
npm install -g wrangler

# Авторизуйтесь
wrangler login

# Создайте KV namespace
wrangler kv:namespace create "TODOS_KV"
```

Скопируйте `id` из вывода команды.

## Шаг 2: Настройка wrangler.toml

Откройте `wrangler.toml` и замените `YOUR_KV_NAMESPACE_ID` на реальный ID:

```toml
[[kv_namespaces]]
binding = "TODOS_KV"
id = "ваш_реальный_id_здесь"
```

## Шаг 3: Деплой Workers

### Вариант A: Через Cloudflare Dashboard

1. Откройте: https://dash.cloudflare.com/pages
2. Выберите проект `ido` (или создайте новый)
3. В настройках проекта → **Functions**
4. Убедитесь, что включены **Functions**
5. Загрузите файл `functions/api/todos.js` в папку `functions/api/` вашего проекта

### Вариант B: Через Git (рекомендуется)

1. Убедитесь, что файл `functions/api/todos.js` в репозитории
2. Сделайте `git push`
3. Cloudflare Pages автоматически подхватит Functions из папки `functions/`

### Вариант C: Через Wrangler CLI

```bash
# Из корня проекта
wrangler pages deploy . --project-name=ido
```

## Шаг 4: Привязка KV к Workers

### Если деплоите через Dashboard:

1. Откройте проект в Cloudflare Pages
2. **Settings** → **Functions** → **KV Namespace Bindings**
3. Нажмите **"Add binding"**
4. **Variable name**: `TODOS_KV`
5. **KV namespace**: выберите созданный `TODOS_KV`
6. Нажмите **"Save"**

### Если деплоите через Wrangler:

KV уже привязан через `wrangler.toml`, ничего делать не нужно.

## Шаг 5: Проверка работы

1. Откройте ваш сайт: `https://ido.pages.dev`
2. Откройте консоль браузера (F12)
3. Добавьте задачу
4. Должно появиться: `✓ Данные синхронизированы с Cloudflare`

### Проверка синхронизации:

1. Откройте туду-лист на компьютере
2. Добавьте задачу
3. Подождите 2-3 секунды (debounce)
4. Откройте тот же URL на телефоне
5. Задача должна появиться автоматически (или через 30 секунд при периодической синхронизации)

## Шаг 6: Проверка данных в KV

1. Откройте: https://dash.cloudflare.com/
2. **Workers & Pages** → **KV**
3. Выберите ваш namespace `TODOS_KV`
4. Должны увидеть ключи вида `todos:user_1234567890_abc123`
5. Кликните на ключ → увидите JSON с задачами

## 🔒 Безопасность

**Текущая реализация:**
- Любой может читать/писать данные (нет аутентификации)
- Подходит для личного использования

**Для продакшена добавьте:**
- Аутентификацию через Cloudflare Access
- Или проверку токена в Worker
- Или используйте userId как секретный ключ

## 💰 Стоимость

**Бесплатный план:**
- ✅ 100,000 чтений в день
- ✅ 1,000 записей в день
- ✅ 1 GB хранения

**Для личного туду-листа более чем достаточно!**

## ⚙️ Оптимизация

Код уже оптимизирован:
- ✅ Debounce 2 секунды (экономит операции записи)
- ✅ Периодическая синхронизация каждые 30 секунд
- ✅ Локальное сохранение для быстрого отклика

## 🐛 Troubleshooting

### Проблема: "KV namespace not configured"

**Решение:**
- Убедитесь, что KV namespace создан
- Проверьте, что ID правильный в `wrangler.toml`
- Если деплоите через Dashboard, проверьте привязку в настройках Functions

### Проблема: Данные не синхронизируются

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки
3. Убедитесь, что API доступен: `https://ido.pages.dev/api/todos?userId=test`
4. Должен вернуть `[]` или массив задач

### Проблема: "Too many requests"

**Решение:**
- Вы превысили лимит бесплатного плана
- Увеличьте debounce до 5 секунд
- Или перейдите на платный план ($5/месяц)

## ✅ Готово!

После настройки:
- ✅ Данные синхронизируются между всеми устройствами
- ✅ Работает офлайн (локальное сохранение)
- ✅ Автоматическая синхронизация при подключении
- ✅ Бесплатно для личного использования

---

**Важно:** Не коммитьте `wrangler.toml` с реальным KV ID в публичный репозиторий, если это приватные данные. Для публичного репо это нормально.

