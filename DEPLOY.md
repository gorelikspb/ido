# 🚀 Деплой туду-листа на Cloudflare Pages

Простая инструкция для деплоя на Cloudflare Pages - один URL для всех устройств!

## Быстрый деплой

### Вариант 1: Через Cloudflare Dashboard (рекомендуется)

1. **Подготовьте репозиторий на GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/gorelikspb/ido.git
   git push -u origin main
   ```

2. **Откройте Cloudflare Pages**
   - Перейдите: https://dash.cloudflare.com/pages
   - Нажмите **"Create a project"** → **"Connect to Git"**

3. **Подключите репозиторий**
   - Выберите: `gorelikspb/ido`
   - Настройки:
     - **Project name**: `ido` (или другое имя)
     - **Production branch**: `main`
     - **Framework preset**: **"None"**
     - **Build command**: оставьте **пустым**
     - **Build output directory**: оставьте **пустым** (файлы в корне)
     - **Root directory**: оставьте пустым

4. **Нажмите "Save and Deploy"**

5. **Готово!** Сайт будет доступен по адресу: `https://ido.pages.dev`

### Вариант 2: Автоматический скрипт

Если у вас настроен Cloudflare API токен в `instructions/.cloudflare-config.local`:

```powershell
.\instructions\scripts\setup-cloudflare-pages-auto.ps1
```

Выберите домен `ido.pages.dev` из списка.

## После деплоя

✅ Откройте сайт на компьютере: `https://ido.pages.dev`  
✅ Откройте тот же URL на телефоне  
✅ Добавьте на главный экран (PWA)  
✅ Данные сохраняются локально на каждом устройстве

## Важно

- **Данные локальные**: Каждое устройство хранит свои задачи в localStorage
- **Один URL**: Один и тот же сайт доступен с любого устройства
- **PWA**: Можно установить как приложение на телефон

## Если нужна синхронизация между устройствами

Можно добавить позже через:
- Firebase Realtime Database (бесплатно)
- Supabase (бесплатно)
- Свой простой бэкенд

Но для начала локальное хранение на каждом устройстве - это нормально и работает офлайн!

---

**Готово!** Теперь у вас есть туду-лист доступный с любого устройства по одному URL 🎉

